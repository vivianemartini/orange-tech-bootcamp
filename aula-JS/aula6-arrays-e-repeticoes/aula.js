// arrays

/*
const alunos = ['joao', 'vitor', 'marina'] //conjunto de dados

alunos.push('Renan') //dinamicamente
console.log(alunos[3])


alunos[4] = 'Vini'

alunos.pop() // tira o ultimo da lista
alunos.shift()// remove o primeiro
*/

//repetição
/*
const notas = []

notas.push(5);
notas.push(10);
notas.push(10);

let soma = 0;

for (let i = 0; i < notas.length; i++) {
    const nota = notas[i];
    soma = soma + nota
    
}

const media = soma / notas.length;
console.log(media.toFixed(1))
*/


const notas = []

notas.push(5);
notas.push(10);
notas.push(10);

let soma = 0;

for (let i = 0; i < notas.length; i++) {
    const nota = notas[i];
    soma = soma + nota
    
}

const media = soma / notas.length;
console.log(media.toFixed(1))